{%- liquid
  assign kicker = kicker | default: ''
  assign title = title | default: ''
  assign text = text | default: ''
  assign align = align | default: 'left'
  assign inverse = inverse | default: false
  assign heading_tag = heading_tag | default: 'h2'
-%}

<div class="theme-section-heading theme-section-heading--{{ align }}{% if inverse %} theme-section-heading--inverse{% endif %}">
  {%- if kicker != blank -%}
    <p class="theme-section-heading__kicker">{{ kicker }}</p>
  {%- endif -%}
  {%- if title != blank -%}
    <{{ heading_tag }} class="theme-section-heading__title">{{ title }}</{{ heading_tag }}>
  {%- endif -%}
  {%- if text != blank -%}
    <div class="theme-section-heading__text rte">{{ text }}</div>
  {%- endif -%}
</div>
